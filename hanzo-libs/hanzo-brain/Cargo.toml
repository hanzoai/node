[package]
name = "hanzo-brain"
version = "0.1.0"
edition = "2021"
description = "Hanzo Brain — pure-CPU algorithm primitives consumed by the Hanzo Node. Cross-runtime byte-equivalent with @hanzo/bot-memory (TS), hanzo-memory (Python), bot-go (Go), and bot-cpp (C++). See hanzoai/brain for the canonical contract."
license = "MIT"

[dependencies]
sha2 = "0.10"
once_cell = "1.19"
